387. First Unique Character in a String

Given a string s, find the first non-repeating character in it and return its index. If it does not exist, return -1.

 

Example 1:

Input: s = "leetcode"

Output: 0

Explanation:

The character 'l' at index 0 is the first character that does not occur at any other index.

Example 2:

Input: s = "loveleetcode"

Output: 2

Example 3:

Input: s = "aabb"

Output: -1 

Constraints:

1 <= s.length <= 105
s consists of only lowercase English letters.

url: https://leetcode.com/problems/first-unique-character-in-a-string/description/

---

---

step1
```py
class Solution:
    def firstUniqChar(self, s: str) -> int:
        char_to_index = {}

        for index, char in enumerate(s):
            if char in char_to_index:
                char_to_index[char] = None
                continue
            char_to_index[char] = index

        valid_char_list = []
        for value in char_to_index.values():
            if value is not None:
                valid_char_list.append(value)

        if not valid_char_list:
            return -1
        return min(valid_char_list)

"""
1回目考えたやつ
key:文字
value:インデックス
1.文字列を全て辞書にいれる
2.重複していたらそのインデックスをNoneにする。
3.辞書のkeyとvalueを入れ替えてsortする。
4.最初のkeyを取り出して吐き出す
5.全て重複していれば-1を返す
"""

"""
2回目考えて、実装したやつ
key:文字
value:インデックス
1.文字列を全て辞書にいれる
2.重複していたらそのインデックスをNoneにする。
3.辞書のvalueをもとにsortする。
4.minでvalueをもとめる。つまりインデックスが得られる
5.全て重複していれば、つまりNoneだったら-1を返す
"""
```
とりあえず考えたことを無理やりコードにした。  
最初は単純にminで一気に調べられると思っていたが、そういえばNoneは比較できなかったのでリストに入れて迂回した。  
あまり筋がいい気がしない。  

文字列の長さをnとする。  
時間計算量: for文で回すのでO(n)、残りは定数項なので無視する
空間計算量: 辞書やリスト作成をするが、最大でもアルファベットの全種類26文字分だけなので定数項、O(1)

