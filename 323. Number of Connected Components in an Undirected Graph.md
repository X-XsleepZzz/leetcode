323. Number of Connected Components in an Undirected Graph
leetcodeではpremium問題のため代替としてneetcodeのリンクを貼る。
問題: https://neetcode.io/problems/count-connected-components/question

You have an undirected graph of n nodes labeled from 0 to n - 1. You are given an integer n and an array edges where edges[i] = [aᵢ, bᵢ] indicates that there is an edge between aᵢ and bᵢ in the graph.

Return the number of connected components in the graph.

Input:
n = 5, edges = [[0,1],[1,2],[3,4]]

Output: 2

Input:
n = 5, edges = [[0,1],[1,2],[2,3],[3,4]]

Output: 1

Constraints:

1 <= n <= 2000
1 <= edges.length <= 5000
edges[i].length == 2
0 <= aᵢ < n
0 <= bᵢ < n
aᵢ != bᵢ
There are no repeated edges
---

この問題以降はodaさんの勧め通り、完走者のstep3を模写し、step2は誰かがまとめたものを読む程度にする。
基本的にolsen_blueさんのコードを参考にする。
https://github.com/olsen-blue/Arai60/blob/db6b6fd116cd087a45911b01a480168cf288d4bd/323.%20Number%20of%20Connected%20Components%20in%20an%20Undirected%20Graph.md

step3
1週目
1回目 false
for next_node in adjacency_list[node]を next_node = adjancy_list[node]と書いて失敗。
これだとリストが変数に入ってしまうので、setに入れられない。
リストの中身を取り出すためにforを使っている。

2週目
1回目 false
adjancyをadjacnyと書くタイポが多い
またfor node1, node2 = edgesと書いてしまった。

3週目
1回目 pass 4m13s
2回目 pass 4m7s
3回目 pass 4m3s
```py
class Solution:
    def countComponents(self, n: int, edges: List[List[int]]) -> int:
        adjacency_list = defaultdict(list)
        for node1, node2 in edges:
            adjacency_list[node1].append(node2)
            adjacency_list[node2].append(node1)
        
        visited_nodes = set()
        def traverse_connected_nodes(node):
            if node in visited_nodes:
                return
            visited_nodes.add(node)
            for next_node in adjacency_list[node]:
                traverse_connected_nodes(next_node)
        
        num_connected_nodes = 0
        for node in range(n):
            if node in visited_nodes:
                continue
            traverse_connected_nodes(node)
            num_connected_nodes += 1
        return num_connected_nodes
```
componentsをnodesという名前に変更。

