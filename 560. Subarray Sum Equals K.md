560. Subarray Sum Equals K
url: https://leetcode.com/problems/subarray-sum-equals-k/description/

Given an array of integers nums and an integer k, return the total number of subarrays whose sum equals to k.

A subarray is a contiguous non-empty sequence of elements within an array.

 

Example 1:

Input: nums = [1,1,1], k = 2
Output: 2
Example 2:

Input: nums = [1,2,3], k = 3
Output: 2
 

Constraints:

1 <= nums.length <= 2 * 10^4
-1000 <= nums[i] <= 1000
-107 <= k <= 107

step1 time Limit Exceededで失敗
```py
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        subarray_count = 0
        total_sum = 0

        for start in range(len(nums)):
            for i in nums[start:]:
                total_sum += i
                if total_sum == k:
                    subarray_count += 1
            total_sum = 0
        return subarray_count
        

"""
1. 先頭から順番に足し合わせてkと同じ値になったらsubarray_countを1加算
2. 先頭の次の要素を先頭に設定する
3. 1、2をループし、最後の要素を先頭に1の処理を行ったらsubarray_countの値を返す
"""         

```

一番、下のコメントは解いている時に頭を整理するために書いたもの。
時間計算量はforループのところでO(n^2) (リスト内の要素数をnとする。)
nums.length <= 2 * 10^4なので大体10^8くらい。遅い。やはりpythonで2重ループは危ない。

・先頭を1つずらして走査する方法
for start in range(len(list)):
 for i in list[start:]

・最初のエラー
最初以下のtotal_sumを加算するところをif文の後に書いていてエラーを起こした。
具体例を想像して見直しはしたはずだがまだ自分の頭でしっかり計算せずに大まかに形がそれっぽいという基準で判定してしまっていた。

```py
total_sum += i
if total_sum == k:
 subarray_count += 1
```

以下はchatGPT 5.6 solのコード
```py
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        prefix_count = {0: 1}
        prefix_sum = 0
        subarray_count = 0

        for num in nums:
            prefix_sum += num

            if prefix_sum - k in prefix_count:
                subarray_count += prefix_count[prefix_sum - k]

            prefix_count[prefix_sum] = prefix_count.get(prefix_sum, 0) + 1

        return subarray_count
```
ぱっと見わからなかったが対話して少し分かった。
現在の累積和 - 過去の累積和 = k
つまり
過去の累積和 = 現在の累積和 - k

num = [1 2 1 1]
k = 3
とすると
[ 1 | 2 1 | 1 ]
    ↑     ↑
この2つの区間の中身の合計がk
右の縦棒から左端までが現在の累積和
左の縦棒から左端までが過去の累積和

私の最初の解法はstartの位置を全て走査させていたが、これは同じ場所を何度も計算し直してしまい効率が悪い。
累積和を使うことで区間内を1回走査して計算をO(n)回する。
つまり、同じ配列の区間内の要素を何回も合計するなら累積和を用いるほうが効率がいい。
その計算をハッシュマップで保存しておく。
部分区間内の合計がkなので、あとは現在の累積和 - k、つまり過去の累積和の値が今までに存在していればハッシュマップで検索する。
そしてsubarray_countを1個増やす。



https://github.com/kazizi55/coding-challenges/pull/16
さんのstep3のコードも読んだ。
アイディアは同じで累積和とハッシュマップを用いる方法。
gptとの違いはdefaultdictを使うかどうか。
使った方がコードは短くなるけど、どっちでもいいかな。個人的には使わない方が好みかもしれない。

---
step2
```py
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        prefix_count =  {0: 1}
        prefix_sum = 0
        subarray_count = 0

        for num in nums:
            prefix_sum += num
            if prefix_sum - k in prefix_count:
                subarray_count += prefix_count[prefix_sum - k]
            prefix_count[prefix_sum] = prefix_count.get(prefix_sum, 0) + 1
        return subarray_count
```
何も見ずに書いていたら
subarray_count += prefix_count[prefix_sum - k]
を
subarray_count += 1
と書く間違いをした。
頭の中でまだイメージできていなかった。
for文で区間内の右の縦棒を右に一個ずつずらしていく、その時々で右の縦棒を固定して現在の総和 - k = 過去の総和となる
左の縦棒の位置は何個あるかという問題なので、辞書内にある登録された数を用いるべき。

時間計算量はリスト内の要素数をnとすると累積和を求めるために1周するのでO(n)。
空間計算量は辞書を保存するので、累積和の種類が全て重複なしならO(n)。
この解法を頭に入れたいのでstep3もこれでいく。

---
step3
1回目 3m50s
2回目 3m45s
3回目 3m40s

```py
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        prefix_count = {0: 1}
        prefix_sum = 0
        subarray_count = 0

        for num in nums:
            prefix_sum += num
            if prefix_sum - k in prefix_count:
                subarray_count += prefix_count[prefix_sum - k]
            prefix_count[prefix_sum] = prefix_count.get(prefix_sum, 0) + 1
        return subarray_count
```





